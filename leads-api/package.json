{
  "name": "enix-leads-api",
  "version": "0.1.0",
  "private": true,
  "description": "Public lead-intake endpoints for Enix Exteriors, deployed to Zo Space (Hono on Bun).",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.10",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
